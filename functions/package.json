{
  "name": "devj-tutor-functions",
  "description": "Cloud Functions for DevJ Tutor — daily streak reminders via FCM",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "npx -y firebase-tools deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0"
  }
}
